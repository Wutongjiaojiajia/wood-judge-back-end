name: buildCI
on:
  push:
    branches: # 当推送分支时
      - master
jobs:
	build:
		runs-on: ubuntu-latest
		steps:
			# 将代码拷贝到虚拟机
			- uses: actions/checkout@v2
			- name: Run a one-line script
			run: echo Hello,world